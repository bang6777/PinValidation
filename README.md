# Structure
├── README.md
├── src
    ├── components
    └── vite-env.d.ts
├── .env
└── vite.config.ts 


# Run source
yarn && yarn dev: the first time run, after that just run "yarn dev"

# Run deploy vercel


# Run Unit test
yarn run test

